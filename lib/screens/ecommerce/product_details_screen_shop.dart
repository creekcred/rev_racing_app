import 'package:flutter/material.dart';

class ProductDetailsScreenShop extends StatelessWidget {
  const ProductDetailsScreenShop({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("ProductDetailsShop")),
      body: Center(
        child: Text("ProductDetailsShop Content"),
      ),
    );
  }
}
